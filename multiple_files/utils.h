// header guard

#ifndef UTILS_H //if not defined and can be named whatever but name it the name of the file
#define UTILS_H // defines it as nothing


double average(double a[], int size); // function prototype 

#endif

//#define PI 3.14 this is a macro this doesnt take space in memory unlike a variable PI = 3.14 it gets baked into the instructions just copys and pastes it to PI use in embedded systems examples